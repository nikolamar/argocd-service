{
  "name": "argocd-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "start": "npx ts-node src/app.ts",
    "start:prod": "node dist/app.js",
    "start:docker": "docker compose --env-file .env up --build",
    "build:docker": "docker build -t argocd-service ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@loopback/build": "^9.0.7",
    "@loopback/eslint-config": "^13.0.7",
    "@types/express": "^4.17.16",
    "eslint": "^8.33.0",
    "ts-node": "^10.9.1"
  }
}
