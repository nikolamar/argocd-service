apiVersion: apps/v1
kind: Deployment

# deploy
metadata:
  name: myapp-deploy

spec:
  # replicas
  replicas: 2
  selector:
    matchLabels:
      app: myapp
  # upgrade pod strategy
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  # pod template
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: myapp
          image: nikolamar/svc-nodejs:0.0.1 # use your image
          ports:
            - containerPort: 3000
