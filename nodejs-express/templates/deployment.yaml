apiVersion: apps/v1
kind: Deployment

# deploy
metadata:
  name: nodejs-express

spec:
  # replicas
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  # upgrade pod strategy
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  # pod template
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: myapp
          image: nikolamar/nodejs-express:0.0.2 # use your image
          ports:
            - containerPort: 3000
